table:
  name: comment_like
  schema: public
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - transaction
    - like
    - created_at
    - user
    - id
    - updated_at
    - rating_id
    filter: {}
  role: anonymous
