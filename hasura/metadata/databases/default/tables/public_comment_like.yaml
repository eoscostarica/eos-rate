table:
  schema: public
  name: comment_like
select_permissions:
  - role: anonymous
    permission:
      columns:
        - transaction
        - like
        - created_at
        - user
        - id
        - updated_at
        - rating_id
      filter: {}
      allow_aggregations: true
