export default theme => ({
  boxColor: {
    float: 'right',
    width: `${theme.spacing(3)} !important`,
    height: `${theme.spacing(3)} !important`,
    borderRadius: '2px',
    border: '2px solid'
  }
})
